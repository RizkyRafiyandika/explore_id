import 'package:flutter/material.dart';

class MydetailPlace extends StatelessWidget {
  const MydetailPlace({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Bali Seafood")));
  }
}
